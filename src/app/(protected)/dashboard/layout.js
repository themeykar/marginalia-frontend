export const metadata = {
  title: "Your Shelf",
  description:
    "Your personal library and reading ledger. Keep track of what is open on your nightstand, in your hands, and finished.",
};

export default function DashboardLayout({ children }) {
  return children;
}
